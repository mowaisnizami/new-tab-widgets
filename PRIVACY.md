# Privacy

New Tab Widgets does not collect, transmit, sell or share personal data.

The extension requests only Chrome's `storage` permission. Dashboard preferences, shortcuts and custom icons remain in the user's local browser profile. Import and export happen only when the user explicitly selects or downloads a JSON configuration file. The extension contains no analytics, advertising, remote scripts or network API calls.
